name: Testes Unitários
on:
  pull_request:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v2

      - name: Instalar dependências
        run: npm install

      - name: Executar testes
        id: test
        run: npm test

      - name: Notificar sucesso
        if: success()  # Este passo será executado apenas se os testes passarem
        run: |
         '{
            "text": "✅ Todos os testes passaram no repositório Sistema de Login em pull request #${{ github.event.pull_request.number }}. Ótimo trabalho!"
          }' 

      - name: Notificar falha
        if: failure()  # Este passo será executado apenas se os testes falharem
        run: |
         '{
            "text": "🚨 Os testes falharam no repositório Sistema de Login em pull request #${{ github.event.pull_request.number }}. Confira os logs para mais detalhes."
          }' 
